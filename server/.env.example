# GYMOS Backend Environment Variables (V1)
NODE_ENV=development
PORT=8080

# Database Configuration (PostgreSQL)
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL=postgres://gymos_user:securepassword@localhost:5432/gymos

# Frontend URL for CORS (Required for secure cookies)
FRONTEND_URL=http://localhost:5173